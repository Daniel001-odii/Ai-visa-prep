// server/api/auth/logout.post.ts
/* import { defineEventHandler, clearCookie } from 'h3';

export default defineEventHandler((event) => {
  clearCookie(event, 'token');
  return { message: 'Logged out successfully' };
}); */